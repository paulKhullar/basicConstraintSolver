the following code can be ran by running the main method inside the SolverExecutor class
packages are divided into generators, intances, representations (ie. binary tuples, constraints etc.), and solvers.